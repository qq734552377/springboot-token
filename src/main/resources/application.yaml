spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3980/repair?useSSL=false
    username: root
    password: root
mybatis:
  config-location: classpath:mybatis.xml
debug: true
server:
  port: 8888